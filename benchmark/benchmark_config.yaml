# benchmark_config.yaml

path_kind: "sin"

# N scaling (row 1)
Ns: [2, 3, 4]

# d scaling (row 2)
Ds: [2, 4, 8]

# m scaling (row 3)
Ms: [2, 3, 4]

operations: ["logsignature", "signature"]

runs_dir: "runs"

# 10 is enough for smoothing; 20 can get very slow with big configs
repeats: 10

logsig_method: "S"
